import { ZoomDirectiveDirective } from './zoom-directive.directive';

describe('ZoomDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
